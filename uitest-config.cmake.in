get_filename_component(UITEST_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

include(CMakeFindDependencyMacro)

if(NOT TARGET uitest)
  include("${UITEST_CMAKE_DIR}/uitest-targets.cmake")
endif()

# EOF #
